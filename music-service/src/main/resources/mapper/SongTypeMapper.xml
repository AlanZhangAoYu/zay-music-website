<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.SongTypeMapper">
    <select id="selectAllSongType" resultType="pojo.SongType">
        select `song_type_tab`.`songTypeId`,`song_type_tab`.`songTypeName` from `song_type_tab`
    </select>
</mapper>